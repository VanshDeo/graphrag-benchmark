services:
  graphrag:
    platform: linux/amd64
    image: tigergraph/graphrag:latest
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      SERVER_CONFIG: /code/configs/server_config.json
      TG_GSQL_PORT: "443"
    volumes:
      - ./src/configs:/code/configs:ro
      - ./data:/code/data:ro
      - ./graphrag/common:/common:ro
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    networks:
      - app-network
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: src/server/Dockerfile
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - PORT=8080
      - GRAPHRAG_SERVICE_URL=http://graphrag:8000
    depends_on:
      - graphrag
    volumes:
      - ./results:/app/results
      - ./data:/app/data
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    build:
      context: ./src/frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3001"
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
